version: '3'

services:
  postgres:
    image: postgres:9.4
    container_name: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_DB=springraphql
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_USER=postgres